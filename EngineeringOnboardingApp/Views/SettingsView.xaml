<UserControl x:Class="EngineeringOnboardingApp.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="1400" d:DesignHeight="820"
             Background="Transparent">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="26,20">
        <StackPanel>
            <TextBlock Text="Settings" Style="{StaticResource H1TextStyle}"/>
            <TextBlock Text="Manage application state and about information."
                       Style="{StaticResource SubtitleTextStyle}" Margin="0,4,0,0"/>

            <!-- Progress -->
            <Border Style="{StaticResource CardStyle}" Margin="0,18,0,0">
                <StackPanel>
                    <TextBlock Text="Reset Onboarding Progress" Style="{StaticResource H2TextStyle}"/>
                    <TextBlock Text="Clears saved step and tool state. Configuration files are not affected."
                               Style="{StaticResource SubtitleTextStyle}" Margin="0,4,0,0"/>
                    <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                        <Button Content="Reset All Progress" Style="{StaticResource DangerButtonStyle}" Click="ResetProgress_Click"/>
                        <Button Content="Save Progress Now" Style="{StaticResource EngButtonStyle}" Margin="10,0,0,0" Click="SaveProgress_Click"/>
                    </StackPanel>
                    <TextBlock x:Name="ResetStatus" Foreground="#9AA2B1" FontSize="12" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- Role -->
            <Border Style="{StaticResource CardStyle}" Margin="0,14,0,0">
                <StackPanel>
                    <TextBlock Text="My Role" Style="{StaticResource H2TextStyle}"/>
                    <TextBlock x:Name="RoleStatus" Style="{StaticResource SubtitleTextStyle}" Margin="0,4,0,0"/>
                    <TextBlock Text="Your role determines which steps apply to you (e.g. the E5 license request for contractors)."
                               Foreground="#9AA2B1" FontSize="12" Margin="0,4,0,0" TextWrapping="Wrap"/>
                    <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                        <Button Content="Contractor" Style="{StaticResource EngButtonStyle}" Click="Contractor_Click"/>
                        <Button Content="Direct Employee" Style="{StaticResource EngButtonStyle}" Margin="10,0,0,0" Click="Employee_Click"/>
                    </StackPanel>
                    <TextBlock x:Name="RoleStatusText" Foreground="#9AA2B1" FontSize="12" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>

            <!-- Data -->
            <Border Style="{StaticResource CardStyle}" Margin="0,14,0,0">
                <StackPanel>
                    <TextBlock Text="Application Data" Style="{StaticResource H2TextStyle}"/>
                    <TextBlock Text="State file and configuration locations." Style="{StaticResource SubtitleTextStyle}" Margin="0,4,0,0"/>
                    <TextBlock x:Name="StatePathText" Foreground="#C4CBD9" FontSize="12" Margin="0,12,0,0" TextWrapping="Wrap"/>
                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <Button Content="Open Data Folder" Style="{StaticResource EngButtonStyle}" Click="OpenData_Click"/>
                        <Button Content="Copy Path" Style="{StaticResource EngButtonStyle}" Margin="10,0,0,0" Click="CopyPath_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- About -->
            <Border Style="{StaticResource CardStyle}" Margin="0,14,0,0" Background="{StaticResource HeroGradientBrush}">
                <StackPanel>
                    <TextBlock Text="About" Style="{StaticResource H2TextStyle}"/>
                    <TextBlock Text="Engineering Onboarding Â· GE HealthCare Ultrasound Systems" Style="{StaticResource SubtitleTextStyle}" Margin="0,6,0,0"/>
                    <TextBlock Text="Version 1.0.0" Foreground="#9AA2B1" FontSize="12" Margin="0,8,0,0"/>
                    <TextBlock Text="Guides new engineers through provisioning a workstation for VisualSonics / Voluson development." Foreground="#B49BD0" FontSize="12" TextWrapping="Wrap" Margin="0,6,0,0"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
