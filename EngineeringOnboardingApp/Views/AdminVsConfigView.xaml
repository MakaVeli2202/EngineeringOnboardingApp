<UserControl x:Class="EngineeringOnboardingApp.Views.AdminVsConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="1400" d:DesignHeight="820"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title + actions -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Visual Studio Configurations" Style="{StaticResource H1TextStyle}"/>
                <TextBlock Text="Maintain the .vsconfig files used to provision a development workstation."
                           Style="{StaticResource SubtitleTextStyle}" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Upload File" Style="{StaticResource EngButtonStyle}" Margin="0,0,10,0" Click="Upload_Click"/>
                <Button Content="Export" Style="{StaticResource EngButtonStyle}" Margin="0,0,10,0" Click="Export_Click"/>
                <Button Content="Save Changes" Style="{StaticResource PrimaryButtonStyle}" Click="Save_Click"/>
            </StackPanel>
        </Grid>

        <!-- Target selector + summary -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,16,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="Btn2022" Content="Visual Studio 2022" Style="{StaticResource FeatureCardStyle}" Foreground="#ECEEF4" Cursor="Hand" Click="Target2022_Click" Margin="0,0,10,0"/>
                    <Button x:Name="Btn2026" Content="Visual Studio 2026" Style="{StaticResource FeatureCardStyle}" Foreground="#ECEEF4" Cursor="Hand" Click="Target2026_Click"/>
                </StackPanel>
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,14,0,0">
                    <TextBlock x:Name="SummaryText" Foreground="#C4CBD9" FontSize="12" VerticalAlignment="Center"/>
                    <TextBlock x:Name="VersionText" Foreground="#7C8AA5" FontSize="12" Margin="16,0,0,0" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>

        <TextBlock x:Name="StatusText" Grid.Row="2" FontSize="12" TextWrapping="Wrap"
                   VerticalAlignment="Top" Margin="2,14,0,0" Visibility="Collapsed"/>

        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Padding="0" Margin="0,14,0,0">
            <DockPanel>
                <Border DockPanel.Dock="Top" Padding="16,12" BorderBrush="#1E232E" BorderThickness="0,0,0,1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="WORKLOAD COMPONENTS" Style="{StaticResource SectionLabelTextStyle}" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBox x:Name="AddBox" Width="420" Height="30" Margin="0,0,8,0" PreviewKeyDown="AddBox_KeyDown"/>
                            <Button Content="+ Add" Style="{StaticResource EngButtonStyle}" Click="Add_Click"/>
                        </StackPanel>
                    </Grid>
                </Border>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                              Padding="14,10">
                    <StackPanel x:Name="ComponentsPanel"/>
                </ScrollViewer>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>