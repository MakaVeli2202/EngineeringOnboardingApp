<UserControl x:Class="EngineeringOnboardingApp.Views.ValidationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="1400" d:DesignHeight="820"
             Background="Transparent">
    <Grid Margin="26,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Environment Validation" Style="{StaticResource H1TextStyle}"/>
                <TextBlock Text="Run preflight checks to verify the environment is ready before setup."
                           Style="{StaticResource SubtitleTextStyle}" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="Run Validation" Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding RunCommand}"/>
                <Button Content="Clear" Style="{StaticResource EngButtonStyle}" Margin="10,0,0,0"
                        Command="{Binding ClearCommand}"/>
            </StackPanel>
        </Grid>

        <!-- Stats -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,16,0,0" Padding="16,12">
            <StackPanel Orientation="Horizontal">
                <Border CornerRadius="8" Background="#14251B" Padding="14,8" Margin="0,0,12,0">
                    <TextBlock x:Name="OkBadge" FontWeight="Bold" Foreground="#22C55E" FontSize="14"/>
                </Border>
                <Border CornerRadius="8" Background="#261F12" Padding="14,8" Margin="0,0,12,0">
                    <TextBlock x:Name="WarnBadge" FontWeight="Bold" Foreground="#F5A623" FontSize="14"/>
                </Border>
                <Border CornerRadius="8" Background="#261316" Padding="14,8">
                    <TextBlock x:Name="ErrorBadge" FontWeight="Bold" Foreground="#F45B69" FontSize="14"/>
                </Border>
                <TextBlock x:Name="StatusText" Foreground="#9AA2B1" FontSize="12" VerticalAlignment="Center" Margin="16,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Results -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0" Margin="0,16,0,0">
            <DataGrid x:Name="ResultsGrid" AutoGenerateColumns="False" IsReadOnly="True"
                      CanUserResizeRows="False" CanUserAddRows="False"
                      HeadersVisibility="Column" RowHeaderWidth="0" GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#262B36" Background="Transparent" RowBackground="Transparent"
                      AlternatingRowBackground="#101216">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Area" Binding="{Binding Area}" Width="130"/>
                    <DataGridTextColumn Header="Item" Binding="{Binding Item}" Width="220"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="90"/>
                    <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="3" Padding="0" Margin="0,12,0,0">
            <TextBlock x:Name="SummaryText" Foreground="#9AA2B1" FontSize="12"/>
        </Border>
    </Grid>
</UserControl>
