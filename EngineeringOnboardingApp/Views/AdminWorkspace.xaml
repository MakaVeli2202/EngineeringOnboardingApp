<UserControl x:Class="EngineeringOnboardingApp.Views.AdminWorkspace"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignWidth="1400" d:DesignHeight="820"
             Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Admin header + tab bar -->
        <Border Background="Transparent" Padding="0,0,0,14"
                BorderThickness="0,0,0,1" BorderBrush="#1E232E">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Administration" Style="{StaticResource H1TextStyle}" VerticalAlignment="Center"/>
                    <Button x:Name="TabLinks" Content="Content Links" Style="{StaticResource EngButtonStyle}" Margin="18,0,0,0" Click="TabLinks_Click" VerticalAlignment="Center"/>
                    <Button x:Name="TabVsConfig" Content="VS Configs" Style="{StaticResource EngButtonStyle}" Margin="10,0,0,0" Click="TabVsConfig_Click" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="Logout" Style="{StaticResource DangerButtonStyle}" Click="Logout_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <ContentControl x:Name="Host" Grid.Row="1" Margin="0,16,0,0"/>
    </Grid>
</UserControl>